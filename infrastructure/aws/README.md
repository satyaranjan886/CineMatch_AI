# AWS Terraform sketches

Illustrative multi-AZ infrastructure (VPC, ALB, RDS, ElastiCache, S3, ECS notes).

Do not run `terraform apply` unless an operator intentionally targets a reviewed AWS account. Secrets must come from Secrets Manager — never from committed files.
